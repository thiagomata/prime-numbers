package v1.seq.sieve

import org.scalatest.flatspec.*
import org.scalatest.matchers.should.*
import stainless.collection.List
import v1.prime.Prime

class SieveSequenceV0Test extends FlatSpec with Matchers  {

//  "SieveSequenceV0.apply0()" should "return the bigger prime" in {
//    val s0 = SieveSequenceV0(List(Prime(2)))
//    s0.head.value should be(BigInt(2))
//    s0.apply(0) should be(BigInt(2))
//    s0.apply(1) should be(BigInt(3))
//    s0.apply(2) should be(BigInt(5))
//  }

}
