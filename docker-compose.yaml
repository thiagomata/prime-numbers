version: '3.8'

services:
  stainless:
    build: 
      context: .
      dockerfile: ./infra/docker/stainless/Dockerfile
    volumes:
      - ./src:/app/src
    working_dir: /app
    command: ["stainless", "$(find src/main/scala -name '*.scala')"]
